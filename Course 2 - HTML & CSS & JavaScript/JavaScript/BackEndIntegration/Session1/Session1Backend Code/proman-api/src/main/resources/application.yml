server:
  servlet:
    port: 8080
    contextPath: /api

spring:

  application:
    name: proman-api

  datasource:
    url: jdbc:postgresql://192.168.0.118:5332/pmadb
    username: postgres
    password: postgres@123

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
